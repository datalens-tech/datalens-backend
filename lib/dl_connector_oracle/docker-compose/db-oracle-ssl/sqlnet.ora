NAMES.DIRECTORY_PATH = (EZCONNECT, TNSNAMES)

WALLET_LOCATION =
  (SOURCE =
    (METHOD = FILE)
    (METHOD_DATA =
      (DIRECTORY = /opt/oracle/wallet)
    )
  )

DISABLE_OOB=ON

SSL_CLIENT_AUTHENTICATION = FALSE
