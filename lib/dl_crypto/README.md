# dl-crypto

Fernet-based encryption and decryption with multi-key management. Provides `CryptoController` for encrypting/decrypting data using key IDs that enable key rotation, and `CryptoKeysSettings` for loading keys from environment variables.
