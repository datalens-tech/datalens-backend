# dl-sqlalchemy-starrocks

SQLAlchemy dialect for StarRocks. Extends the MySQL dialect with StarRocks-specific DDL (e.g. `DUPLICATE KEY` in `CREATE TABLE`) and type compilation. Used by `dl-connector-starrocks`.
