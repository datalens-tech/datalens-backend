version: '3'

tasks:
  devenv:
    dir: '/{{.USER_WORKING_DIR}}'
    cmds:
      - docker-compose up --build

  devenv-d:
    dir: '/{{.USER_WORKING_DIR}}'
    cmds:
      - docker-compose up --build -d
