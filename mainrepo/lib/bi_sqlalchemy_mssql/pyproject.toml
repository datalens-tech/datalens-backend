
[tool.poetry]
name = "yandex-bi-sqlalchemy-mssql"
version = "0.3"
description = "BI MSSQL SQLAlchemy Dialect"
authors = ["Datalens Backendovich <robot-datalens-back@yandex-team.ru>"]
packages = [{include = "bi_sqlalchemy_mssql"}]
license = "MIT"
readme = "README.rst"


[tool.poetry.dependencies]
pyodbc = ">=4.0.35"
python = ">=3.10, <3.12"
sqlalchemy = ">=1.4.46, <2.0"
yandex-bi-sqlalchemy-common = {path = "../bi_sqlalchemy_common"}

[tool.poetry.plugins."sqlalchemy.dialects"]
bi_mssql = "bi_sqlalchemy_mssql.base:BIMSSQLDialect"

[tool.poetry.group.tests.dependencies]
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra"
testpaths = ["bi_sqlalchemy_mssql_tests/unit"]

[tool.ruff]
line-length = 140
select = [
    # Pyflakes
    "F",
    # Pycodestyle
    "E",
    "W",
    # isort
    "I"
]

[tool.black]
line-length = 140
target-version = ['py310']




[tools.mypy]
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
strict_optional = true
