SQLA_DB_CFG_TEMPLATE = dict(
    host=','.join(PG_HOSTS),
    port=PG_PORT,
    user=PG_USER,
    password=PG_PASSWORD,
    database=PG_DATABASE,
    timeout=60,
    connect_timeout=PG_CONN_TIMEOUT,
    sslmode=PG_SSL_MODE,
    sslrootcert=PG_SSL_ROOT_CERT_PATH
)

SQLA_DB_CFG_ANY = dict(
    **SQLA_DB_CFG_TEMPLATE,
    target_session_attrs='any'
)

SQLA_DB_CFG_MASTER = dict(
    **SQLA_DB_CFG_TEMPLATE,
    target_session_attrs='read-write'
)
